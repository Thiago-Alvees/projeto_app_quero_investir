name: Update Market Fundamentals

on:
  schedule:
    # 21:30 UTC = 18:30 Fortaleza (UTC-3), seg-sex
    - cron: "30 21 * * 1-5"
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout private repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20.19.4"

      - name: Generate market fundamentals snapshot
        env:
          BRAPI_TOKEN: ${{ secrets.BRAPI_TOKEN }}
        run: |
          node scripts/update_market_fundamentals.mjs

      - name: Checkout public snapshot repo
        uses: actions/checkout@v4
        with:
          repository: Thiago-Alvees/app_snapshot
          token: ${{ secrets.SNAPSHOT_REPO_TOKEN }}
          path: snapshot-repo

      - name: Copy fundamentals to public repo
        run: |
          mkdir -p snapshot-repo/data
          cp data/market_fundamentals.json snapshot-repo/data/market_fundamentals.json

      - name: Commit and push to public repo
        run: |
          cd snapshot-repo
          git config user.name "snapshot-bot"
          git config user.email "snapshot-bot@users.noreply.github.com"
          git add data/market_fundamentals.json
          git diff --cached --quiet || git commit -m "chore(snapshot): update market fundamentals"
          git push

